// Code generated by ogen, DO NOT EDIT.

package gen

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// GetICal implements getICal operation.
	//
	// Returns the iCalendar (.ics) file for the user with the given ISU.
	//
	// GET /{isu}/ical
	GetICal(ctx context.Context, params GetICalParams) (GetICalOK, error)
	// GetSchedule implements getSchedule operation.
	//
	// Returns the schedule for the user with the given ISU.
	//
	// GET /{isu}/schedule
	GetSchedule(ctx context.Context, params GetScheduleParams) ([]ScheduleItem, error)
	// HealthCheck implements healthCheck operation.
	//
	// Verifies the API is operational and returns its status.
	//
	// GET /health
	HealthCheck(ctx context.Context) error
	// SubscribeSchedule implements subscribeSchedule operation.
	//
	// Subscribes user by ISU and password, generates and stores iCal file.
	//
	// POST /subscribe
	SubscribeSchedule(ctx context.Context, req *SubscribeRequest) (*SubscribeResponse, error)
	// NewError creates *ErrorStatusCode from error returned by handler.
	//
	// Used for common default response.
	NewError(ctx context.Context, err error) *ErrorStatusCode
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h Handler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		baseServer: s,
	}, nil
}
